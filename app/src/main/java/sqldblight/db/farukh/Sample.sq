CREATE TABLE IF NOT EXISTS sampleEntity (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL
);

getAll:
SELECT *
FROM sampleEntity;

insertUser:
INSERT INTO sampleEntity (name)
VALUES (?);

delete:
DELETE FROM sampleEntity
WHERE id = ?;